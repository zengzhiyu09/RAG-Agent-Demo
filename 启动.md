前期部署：安装python环境，获取apikey放置在环境变量（使用dashscope调用），
安装requirement.txt中的依赖  
``
pip install -r requirements.txt
``

docker开启milvus attu监控

attu默认地址 ：http://localhost:3000/#/

控制台 streamlit run app.py

log会打印在控制台和txt中